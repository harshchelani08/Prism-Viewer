# 📘 Prism-Viewer

**Prism-Viewer** is a lightweight, modern 3D model viewer built with **React**, **Vite**, and **Three.js** (or similar 3D/webGL library).  
It lets you visualize 3D geometry in a responsive web app with smooth interaction and UI.

---

## 🚀 Features

✔ Blazing fast with **Vite + SWC**  
✔ Easy drag-and-drop 3D model loading  
✔ Controls for orbiting, zooming, and panning  
✔ Extensible plugin or UI system  
✔ TypeScript support  
✔ Modular React component structure

---

## 🛠 Tech Stack

| Category | Technology |
|----------|------------|
| Build Tool | Vite |
| UI Library | React |
| Language | TypeScript |
| 3D Renderer | ⚠ *(Replace with actual library, e.g. `Three.js`)* |
| Styling | TailwindCSS / CSS Modules (or your choice) |

---

## 📦 Installation

Make sure you have **Node.js 18+** installed.

```bash
# clone repo
git clone https://github.com/harshchelani08/Prism-Viewer.git
cd Prism-Viewer

# install dependencies
npm install
